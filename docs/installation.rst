============
Installation
============

At the command line::

    $ easy_install flask-obfuscateids

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-obfuscateids
    $ pip install flask-obfuscateids